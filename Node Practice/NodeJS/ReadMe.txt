npm install express@<version_num>
// if want latest then use 'latest' instead of version_num.

npm install nodemon@<version_num>
// nodemon - need not to start the server everytime ..... just save the code and the server will start with modified changes automatically.

// module wrapper functions
(function(exports, require, module, __filename, __dirname) {
  // code of .js file
})

// File System module
In one of the Default Module (FileSystem) we have a pairs of each methods 
1. A-Sync method (preferred to use).
2. Sync method (no to use).